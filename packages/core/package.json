{
  "name": "@ownerview/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared, platform-agnostic domain layer for OwnerView (types, schemas, data). No UI, no native APIs.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
